[package]
name = "thinkpad-rotate"
version = "0.1.0"
edition = "2021"
license = "WTFPL"
description = "Opt-in Sway rotation from the ThinkPad EC accelerometer"

[dependencies]
libc = "0.2"
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
strip = true
